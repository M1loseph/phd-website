[package]
name = "ddns-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = { path = "../lib/dotenv" }
iron = "0.6.1"
# TODO: minimize the feature list
tokio = { version = "1", features = ["full"] }
# TODO: remove this feature if possible (not being used)
reqwest = { version = "0.12", features = ["json"] }
log = "0.4.21"
env_logger = "0.11.2"
prometheus = "0.13.3"
router = "0.6.0"
sysinfo = "0.31.4"
tokio-postgres = "0.7.11"
regex = "1.10.6"
sha2 = "0.10.8"
signal-hook = "0.3.17"
